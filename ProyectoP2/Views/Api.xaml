<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoP2.Views.Api"
             Title="Api">
    
    <StackLayout Padding="10">
        <Label Text="Libro Aleatorio" FontSize="24" HorizontalOptions="Center" />
        <ListView ItemsSource="{Binding ListaProducto}" SelectedItem="{Binding ProductoSeleccionado}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Nombre}" Detail="{Binding Codigo}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Cargar Otro Libro" Command="{Binding EjecutarBusquedaCommand}" />
        <Button Text="Guardar Libro" Command="{Binding GuardarLibroCommand}" />
        <ActivityIndicator IsVisible="{Binding LoadingEsVisible}" IsRunning="{Binding LoadingEsVisible}" />
    </StackLayout>
</ContentPage>